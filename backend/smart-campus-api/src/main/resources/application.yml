spring:
  application:
    name: smart-campus-api
  datasource:
    url: jdbc:mysql://localhost:3306/smart_campus
    username: root
    password: password
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
server:
  port: 8080
jwt:
  secret: your-secret-key
  expiration: 86400

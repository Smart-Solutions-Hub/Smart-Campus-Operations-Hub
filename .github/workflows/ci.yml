name: CI

on:
  push:
    branches: ["main"]
  pull_request:

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder step
        run: echo "Configure CI steps"
